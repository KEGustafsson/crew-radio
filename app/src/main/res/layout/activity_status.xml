<?xml version="1.0" encoding="utf-8"?>
<!-- The toolbar is in the layout, not the window decor, so it can take the status-bar inset
     itself: the theme is NoActionBar and the activity pads this root with the window insets. -->
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:id="@+id/root"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical">

    <com.google.android.material.appbar.MaterialToolbar
        android:id="@+id/toolbar"
        android:layout_width="match_parent"
        android:layout_height="?attr/actionBarSize"
        android:title="@string/menu_status" />

    <ScrollView
        android:id="@+id/scroll"
        android:layout_width="match_parent"
        android:layout_height="0dp"
        android:layout_weight="1"
        android:fillViewport="true">
        <LinearLayout
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:orientation="vertical"
            android:padding="16dp">

            <!-- Same header as the main screen, with the channel state where the count sits there. -->
            <LinearLayout
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:orientation="horizontal"
                android:baselineAligned="false"
                android:gravity="center_vertical">
                <LinearLayout
                    android:layout_width="0dp"
                    android:layout_weight="1"
                    android:layout_height="wrap_content"
                    android:orientation="vertical">
                    <TextView
                        android:layout_width="wrap_content"
                        android:layout_height="wrap_content"
                        android:text="@string/crew_channel"
                        android:fontFamily="monospace"
                        android:letterSpacing="0.18"
                        android:textSize="12sp"
                        android:textColor="@color/text_teal_dim" />
                    <TextView
                        android:id="@+id/crewName"
                        android:layout_width="match_parent"
                        android:layout_height="40dp"
                        android:gravity="center_vertical"
                        android:fontFamily="sans-serif-black"
                        android:letterSpacing="0.1"
                        android:autoSizeTextType="uniform"
                        android:autoSizeMinTextSize="14sp"
                        android:autoSizeMaxTextSize="27sp"
                        android:autoSizeStepGranularity="1sp"
                        android:textColor="@color/primary"
                        android:maxLines="1"
                        android:layout_marginEnd="8dp" />
                </LinearLayout>
                <TextView
                    android:id="@+id/statePill"
                    android:layout_width="wrap_content"
                    android:layout_height="44dp"
                    android:gravity="center"
                    android:paddingStart="14dp"
                    android:paddingEnd="14dp"
                    android:fontFamily="monospace"
                    android:letterSpacing="0.1"
                    android:textSize="13sp"
                    android:textStyle="bold"
                    android:background="@drawable/bg_outline" />
            </LinearLayout>

            <LinearLayout
                android:id="@+id/cards"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:orientation="vertical" />
        </LinearLayout>
    </ScrollView>
</LinearLayout>
